<?php
    //php is_numeric returns whether the parameter is a number or not 
    //can be used for server side validations as well
    $int = 3;
    if (is_numeric($int)) {
        echo "Done";
    };
?>